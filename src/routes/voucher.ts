import { Router } from 'express';
import AuthMiddleware from '../middlewares/auth';
import { checkRole } from '../middlewares/checkRole';
import { RoleEnum } from '../utils/shopp.enum';
import VoucherMiddleware from '../middlewares/voucher';
const routes = Router();

/**
 * @swagger
 * /voucher/list-all:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Get all vouchers
 *   description: Get all vouchers
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/list-all',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.listAll
);

/**
 * @swagger
 * /voucher/list-shopP:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Get vouchers generated by shopP
 *   description: Get vouchers generated by shopP
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/list-shopP',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.listAppVouchers
);

/**
 * @swagger
 * /voucher/list-shop:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Get vouchers generated by shop
 *   description: Get vouchers generated by shop
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/list-shop',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.listShopVouchers
);

/**
 * @swagger
 * /voucher/new:
 *  post:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Create new voucher
 *   description: Create new voucher
 *   requestBody:
 *    content:
 *     application/json:
 *      schema:
 *       $ref: '#/components/schemas/VoucherRequest'
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.post(
  '/new',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.SHOP)],
  VoucherMiddleware.newVoucher
);

/**
 * @swagger
 * /voucher/{id}:
 *  post:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Edit voucher
 *   description: Edit voucher
 *   parameters:
 *    - in: path
 *      name: id
 *      schema:
 *       type: string
 *       format: uuid
 *      required: true
 *      description: id of voucher
 *      example: f191d8ad-3d10-4681-9b14-95d8de1e61e1
 *   requestBody:
 *    content:
 *     application/json:
 *      schema:
 *       $ref: '#/components/schemas/VoucherRequest'
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.post(
  '/:id',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.SHOP)],
  VoucherMiddleware.editVoucher
);

/**
 * @swagger
 * /voucher/delete/{id}:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Delete voucher
 *   description: Delete voucher
 *   parameters:
 *    - in: path
 *      name: id
 *      schema:
 *       type: string
 *       format: uuid
 *      required: true
 *      description: id of voucher
 *      example: f191d8ad-3d10-4681-9b14-95d8de1e61e1
 *   responses:
 *    200:
 *     $ref: '#/components/responses/200OK'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/delete/:id',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.SHOP)],
  VoucherMiddleware.deleteVoucher
);

/**
 * @swagger
 * /voucher/save-voucher/{id}:
 *  post:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Save voucher into customer wallet
 *   description: Save voucher into customer wallet
 *   parameters:
 *    - in: path
 *      name: id
 *      schema:
 *       type: string
 *       format: uuid
 *      required: true
 *      description: id of voucher
 *      example: f191d8ad-3d10-4681-9b14-95d8de1e61e1
 *   responses:
 *    200:
 *     $ref: '#/components/responses/200OK'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.post(
  '/save-voucher/:id',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.saveVoucher
);

/**
 * @swagger
 * /voucher/delete-customer-voucher/{id}:
 *  post:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Delete voucher from customer wallet
 *   description: Delete voucher from customer wallet
 *   parameters:
 *    - in: path
 *      name: id
 *      schema:
 *       type: string
 *       format: uuid
 *      required: true
 *      description: id of voucher
 *      example: f191d8ad-3d10-4681-9b14-95d8de1e61e1
 *   responses:
 *    200:
 *     $ref: '#/components/responses/200OK'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.post(
  '/delete-customer-voucher/:id',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.deleteCustomerVoucher
);

/**
 * @swagger
 * /voucher/customer-ShopP:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Show ShopP vouchers from customer wallet
 *   description: Show ShopP vouchers from customer wallet
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/customer-ShopP',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.showCustomerAppVouchers
);

/**
 * @swagger
 * /voucher/customer-Shop:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Show shop vouchers from customer wallet
 *   description: Show shop vouchers from customer wallet
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/customer-Shop',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.showCustomerShopVouchers
);

/**
 * @swagger
 * /voucher/customer-freeship:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Show freeship shopP vouchers from customer wallet
 *   description: Show freeship shopP vouchers from customer wallet
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/customer-freeship',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.showCustomerFreeshipVouchers
);

/**
 * @swagger
 * /voucher/customer-discount:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Show discount shopP vouchers from customer wallet
 *   description: Show discount shopP vouchers from customer wallet
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherListResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/customer-discount',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.showCustomerDiscountVouchers
);

/**
 * @swagger
 * /voucher/{id}:
 *  get:
 *   tags:
 *    - Voucher
 *   security:
 *    - bearerAuth: []
 *   summary: Find voucher by id
 *   description: Find voucher by id
 *   parameters:
 *    - in: path
 *      name: id
 *      schema:
 *       type: string
 *       format: uuid
 *      required: true
 *      description: id of voucher
 *      example: f191d8ad-3d10-4681-9b14-95d8de1e61e1
 *   responses:
 *    200:
 *     description: Success
 *     content:
 *      application/json:
 *       schema:
 *        $ref: '#/components/schemas/VoucherResponse'
 *    400:
 *     $ref: '#/components/responses/400BadRequest'
 *    401:
 *     $ref: '#/components/responses/401Unauthorized'
 */
routes.get(
  '/:id',
  [AuthMiddleware.checkJwt, checkRole(RoleEnum.CUSTOMER)],
  VoucherMiddleware.getOneById
);

export default routes;
